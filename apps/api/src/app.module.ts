import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { BullModule } from '@nestjs/bullmq';
import { HealthModule } from './health/health.module';
import { MetricModule } from './metric/metric.module';
import { AppConfigModule } from './config/config.module';
import { DatabaseModule } from './database/database.module';
import { CoreExtensionModule } from '@tentacrawl/core';

// generated by @tentacrawl/cli
let apiModules: import('@nestjs/common').DynamicModule[] = [];
try {
  apiModules = require('./generated/modules').apiModules;
} catch {
  // no generated modules yet — run `pnpm generate` to create them
}

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      envFilePath: ['.env.local', '.env'],
    }),
    BullModule.forRootAsync({
      useFactory: (configService: ConfigService) => ({
        connection: {
          host: configService.get('REDIS_HOST', 'localhost'),
          port: configService.get<number>('REDIS_PORT', 6379),
          password: configService.get('REDIS_PASSWORD') || undefined,
        },
      }),
      inject: [ConfigService],
    }),
    AppConfigModule,
    DatabaseModule,
    CoreExtensionModule,
    HealthModule,
    MetricModule,
    ...apiModules,
  ],
})
export class AppModule {}
